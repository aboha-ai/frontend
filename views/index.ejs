<!DOCTYPE html>
<html lang="ko">
    <head>
        <meta charset="UTF-8"/>
        <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
        <title><%= pageTitle %></title> <!-- 동적 제목 삽입 -->
        <link href="https://fonts.googleapis.com/css2?family=Noto+Sans+KR:wght@400;500;700&amp;display=swap" rel="stylesheet"/>
        <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet"/>
        <!-- Tailwind CSS를 먼저 로드 -->
        <link href="https://ai-public.creatie.ai/gen_page/tailwind-custom.css" rel="stylesheet"/>
        <!-- Tailwind JS는 그 후에 로드 -->
        <script src="https://cdn.tailwindcss.com/3.4.5?plugins=forms@0.5.7,typography@0.5.13,aspect-ratio@0.4.2,container-queries@0.1.1"></script>
        <script src="https://ai-public.creatie.ai/gen_page/tailwind-config.min.js" data-color="#000000" data-border-radius="small"></script>
    </head>
    
    <body class="bg-gray-50 font-sans">
        <div data-include-path="./navber.html"></div>
        
        <div class="max-w-8xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
            <div class="mb-8">
                <h1 class="text-3xl font-bold text-gray-900"><%= travelTitle %></h1> <!-- 동적 제목 삽입 -->
                <p class="mt-2 text-gray-600"><%= travelDescription %></p> <!-- 동적 설명 삽입 -->
                
                <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6 mt-8">
                    <% travels.forEach(function(travel) { %> <!-- 동적 여행 데이터 반복 -->
                        <div class="bg-white rounded-lg shadow overflow-hidden">
                            <img src="<%= travel.image %>" alt="<%= travel.title %>" class="w-full h-48 object-cover"/>
                            <div class="p-4">
                                <h3 class="font-bold text-lg mb-2"><%= travel.title %></h3>
                                <p class="text-gray-600 text-sm mb-3"><%= travel.date %></p>
                                <div class="flex justify-between items-center">
                                    <span class="text-sm text-gray-500"><i class="fas fa-map-marker-alt mr-1"></i><%= travel.location %></span>
                                    <button class="text-custom hover:text-custom-dark">
                                        <i class="fas fa-angle-right"></i>
                                    </button>
                                </div>
                            </div>
                        </div>
                    <% }); %>
                </div>

                <div class="flex justify-between">
                    <div class="flex justify-between items-center w-full">
                        <div class="flex space-x-2">
                            <button class="!rounded-button bg-gray-100 text-gray-700 px-4 py-2">
                                <i class="fas fa-chevron-left mr-2"></i>이전
                            </button>
                            <button class="!rounded-button bg-gray-100 text-gray-700 px-4 py-2">
                                다음 <i class="fas fa-chevron-right ml-2"></i>
                            </button>
                            <span class="text-gray-600 self-center ml-4">1-3 / 총 12개</span>
                        </div>
                        <button id="open-modal-btn" class="!rounded-button bg-custom text-white px-6 py-3 flex items-center">
                            새 여행 기록 작성하기<i class="fas fa-plus-circle ml-2"></i>
                        </button>
                    </div>
                </div>
            </div>
            
            <div class="mb-8">
                <h1 class="text-3xl font-bold text-gray-900">나의 일정 기록</h1>
                <p class="mt-2 text-gray-600">내가 작성한 모든 일정 이야기</p>
                <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6 mt-8">
                    <% schedules.forEach(function(schedule) { %> <!-- 동적 일정 데이터 반복 -->
                        <div class="bg-white rounded-lg shadow overflow-hidden">
                            <img src="<%= schedule.image %>" alt="<%= schedule.title %>" class="w-full h-48 object-cover"/>
                            <div class="p-4">
                                <h3 class="font-bold text-lg mb-2"><%= schedule.title %></h3>
                                <p class="text-gray-600 text-sm mb-3"><%= schedule.date %></p>
                                <div class="flex justify-between items-center">
                                    <span class="text-sm text-gray-500"><i class="fas fa-calendar-alt mr-1"></i><%= schedule.location %></span>
                                    <button class="text-custom hover:text-custom-dark">
                                        <i class="fas fa-angle-right"></i>
                                    </button>
                                </div>
                            </div>
                        </div>
                    <% }); %>
                </div>

                <div class="flex justify-between mt-4">
                    <div class="flex justify-between items-center w-full">
                        <div class="flex space-x-2">
                            <button class="!rounded-button bg-gray-100 text-gray-700 px-4 py-2">
                                <i class="fas fa-chevron-left mr-2"></i>이전
                            </button>
                            <button class="!rounded-button bg-gray-100 text-gray-700 px-4 py-2">
                                다음 <i class="fas fa-chevron-right ml-2"></i>
                            </button>
                            <span class="text-gray-600 self-center ml-4">1-3 / 총 9개</span>
                        </div>
                        <button class="!rounded-button bg-custom text-white px-6 py-3 flex items-center">
                            새 일정 기록 작성하기<i class="fas fa-plus-circle ml-2"></i>
                        </button>
                    </div>
                </div>
            </div>
        </div>

        <div data-include-path="./modal.html"></div>
        <div data-include-path="./footer.html"></div>

        <script>
            window.addEventListener('load', function() {
                var allElements = document.getElementsByTagName('*');
                Array.prototype.forEach.call(allElements, function(el) {
                    var includePath = el.dataset.includePath;
                    if (includePath) {
                        var xhttp = new XMLHttpRequest();
                        xhttp.onreadystatechange = function () {
                            if (this.readyState == 4 && this.status == 200) {
                                el.outerHTML = this.responseText;
                                // 동적으로 삽입된 스크립트 실행
                                var scripts = el.getElementsByTagName('script');
                                Array.prototype.forEach.call(scripts, function(script) {
                                    var newScript = document.createElement('script');
                                    newScript.type = 'text/javascript';
                                    newScript.text = script.innerText;
                                    document.body.appendChild(newScript); // 스크립트를 body에 추가
                                });
                            }
                        };
                        xhttp.open('GET', includePath, true);
                        xhttp.send();
                    }
                });
            });
        </script>
        <script src="modal.js"></script>
    </body>
</html>
